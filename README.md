### Instructions to run:
- Clone the repo
- Install node v7.0.0

#### Backend:

- `cd backend`
- `npm install`
- `npm start`

Now backend will listen on port 3000

#### Frontend:

- `cd front`
- `npm install`
- `npm start`

Now browse [http://localhost:3001/](http://localhost:3001/)